/** Local CM base URL, injected by vite.config.ts. */
declare const __SML_CM__: string;
/** Bearer token from `sitecore/.sitecore/user.json`, injected by vite.config.ts in dev only. */
declare const __SML_CM_TOKEN__: string | null;
